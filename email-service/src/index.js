import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);


// create a user account
//create a way for that custmer to to enter their customers info and add to mailing list
// name, message, upload, link
//firestore acct needs auto-generated id 